name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]
env:
  VITE_BASE_URL: ${{ vars.VITE_BASE_URL }}
    
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install
        run: npm install

      - name: Lint
        run: npm run lint

      - name: Build
        env:
          VITE_BASE_URL: ${{ vars.VITE_BASE_URL }}
        run: |
            echo "repository variable : $VITE_BASE_URL"
            npm run build
        
          

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          branch: gh-pages
          folder: dist
